# Model Widgets

MEGA does not automatically create an interactive widget from model-card
metadata. To publish an interactive demo, create a [Space](/docs/hub/spaces)
that pins the model repository revision and explains any input or usage limits.

Keep a model card focused on the artifact and evaluation evidence. Keep demo
code, browser behavior, and interactive secrets in the Space workflow rather
than assuming a repository card executes code.
